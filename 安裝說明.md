# Chat Mode Assistant — 新電腦安裝說明

## 前提條件

| 項目 | 說明 |
|------|------|
| **Windows 10/11** | 僅支援 Windows |
| **Intel Developer Toolkit CLI (`dt`)** | 自行下載DT.exe (https://gfx-assets.intel.com/artifactory/gfx-build-assets/build-tools/devtool-go/latest/artifacts/win64/dt.exe), 然後使用`dt.exe install`安裝, 需確定有加入 PATH |
| **GNAI 存取權限** | 透過 AGS 申請「GNAI Chatbot」存取。
| **啟動GNAI extension** | `dt extensions enable gnai` |
| **GNAI 帳號** | `dt gnai config set-credentials` 已完成設定 |
| **Google Chrome** | 用於載入 Extension |
| **tracefmt 工具**（僅 DisplayDebugger 需要） | ETL log 解析所需。安裝 [Windows SDK](https://developer.microsoft.com/en-us/windows/downloads/windows-sdk/) 後，將 `tracefmt.exe` 加入 PATH（通常位於 `C:\Program Files (x86)\Windows Kits\10\bin\10.0.XXXXXX.0\x64\`） |
| **Python** | **一般使用者不需要**；僅開發人員建置 installer 時才需要 |

---

## 一般使用者安裝（使用 .exe 安裝檔）

### 步驟 0（建議）：執行環境檢查

在正式安裝前，先透過安裝檔內建的功能執行環境檢查，確認所有必要工具已正確安裝：

也可以單獨從開發人員處取得`check_env.exe`。

檢查項目：

| 項目 | 說明 |
|------|------|
| Intel `dt` CLI | 是否在 PATH 中 |
| GNAI 認證 | `dt gnai` 是否可正常使用 |
| sighting | Toolkit 安裝 + Assistant 存取權限 |
| displaydebugger | Toolkit 安裝 + Assistant 存取權限 |
| sherlog | Toolkit 安裝 + Assistant 存取權限 |

每項顯示格式：`Toolkit: installed  |  Assistant: accessible`

> ⚠️ Assistant 存取顯示 **⚠（黃色）** 表示無法自動驗證（dt 需要 TTY），顯示 **✗（紅色）** 表示 Toolkit 未安裝或確定無存取權限。

**自動安裝缺少的 Toolkit**：當某個 Toolkit 顯示「not installed」時，該列下方會出現 **▶ Install** 按鈕，點擊後會開啟 PowerShell 視窗自動執行對應的 register 指令，安裝完成並關閉視窗後會自動重新檢查。對應指令如下：

| Toolkit | 安裝指令 |
|---------|----------|
| sighting | `dt gnai toolkits register intel-sandbox/SightingAssistantTool` |
| displaydebugger | `dt gnai toolkits register intel-sandbox/displaydebugger` |
| sherlog | `dt gnai toolkits register intel-innersource/drivers.gpu.core.sherlog-toolkit` |

**載入建議 Config（強烈建議）**：當 `sighting` Toolkit 檢查通過（顯示 ✓）後，該列下方會出現 **⚙ Apply Recommended Config** 按鈕。點擊後會將建議的 `config.local.json` 寫入 sighting toolkit 的安裝目錄，套用以下設定：

| 設定項目 | 值 | 說明 |
|----------|-----|------|
| `table_output_format` | `true` | 以表格格式輸出分析結果，排版更易讀 |
| `state_tokens_enabled` | `false` | 關閉 state token，避免干擾 Extension 解析 |
| `verbose_progress_updates` | `false` | 關閉冗長進度訊息，減少不必要輸出 |
| `html_report_generation` | `false` | 關閉 HTML 報告，僅使用純文字輸出 |
| `subprocess_pause` | `true`（displaydebugger / sherlog） | 子視窗執行完後暫停，方便手動檢視 |
| `cache.enabled` | `true` | 啟用 RAG cache，加速後續查詢 |

> 套用後按鈕會變為 **✓ Config Applied**，設定檔路徑會顯示在彈窗中確認。若 sighting 尚未安裝，請先點擊 **▶ Install** 完成安裝再套用。

所有項目顯示 ✓ 後再繼續安裝步驟。

### 步驟 1：執行安裝檔

執行 `Chat_Mode_Assistant_Setup.exe`，依照精靈指示完成安裝。

- 預設安裝路徑：`%LOCALAPPDATA%\ChatModeAssistant\`（不需要系統管理員權限）

### 步驟 2：載入 Chrome Extension

安裝完成後，精靈會自動開啟 extension 資料夾：

1. 開啟 Chrome，前往 `chrome://extensions/`
2. 右上角開啟「**開發人員模式**」
3. 點擊「**載入未封裝項目**」→ 選擇安裝目錄內的 `extension\` 資料夾
   - 預設路徑：`%LOCALAPPDATA%\ChatModeAssistant\extension\`
4. 載入後 **Extension ID** 固定為 `pmbnnkfhdkommfpphknjpppmlmbihomi`

> Extension 的 `manifest.json` 內含固定的 `key`，因此 Extension ID 永遠相同，且已預先寫入 Native Messaging 設定，**不需要額外設定 Extension ID**。

### 步驟 3：驗證

1. 重新啟動 Chrome
2. 點擊 Extension icon 開啟 Side Panel
3. 狀態應顯示 "Connected"

---

## 架構概覽

```
Chrome Extension (background.js Service Worker)
   │
   ├─①─ Native Messaging ──► native_host.exe ──背景啟動──► bridge_server.exe
   │     "launch" / "check"                    （隱藏視窗）       │
   │     僅啟動期間使用                  寫入 bridge.port ◄────────┘
   │     （輪詢取得埠號後即不再使用）
   │
   └─②─ HTTP / SSE ──────────────────────────────────────► bridge_server.exe
         取得埠號後直接連線                                        │  winpty / ConPTY
         所有後續通訊皆走此路徑                                    ▼
                                                           dt gnai chat --json
                                                               --assistant <name>
```

**埠號協定**：bridge_server 啟動後將實際埠號（預設 8776，若佔用則自動選空閒埠）寫入 `bridge.port`；native_host 讀取此檔並透過 NM 回傳；background.js 快取到 `chrome.storage.session`，Service Worker 重啟後仍可復用。

**Service Worker Keep-alive**：session 進行中，background.js 每 20 秒自我 ping 一次，避免 Chrome 在 AI 長時間執行 tool 時將 Service Worker 暫停，同時監控 SSE 連線並自動重連。

### Bridge Server 端點

| 方法 | 路徑 | 說明 |
|------|------|------|
| `GET` | `/health` | 健康檢查，回傳 session 狀態與 session_id |
| `POST` | `/session/start` | 建立新 chat session（指定 assistant 與 conversation_id） |
| `POST` | `/session/send` | 傳送訊息到 active session |
| `GET` | `/session/stream` | SSE 串流，接收 AI 回覆事件 |
| `POST` | `/session/stop` | 終止 active session |
| `GET/POST` | `/driver-history` | 讀寫 GFX/GOP driver regression 歷史紀錄 |
| `POST` | `/driver-history/delete` | 刪除指定 driver history 條目 |
| `POST` | `/build-cache/lookup` | 查詢 per-version build 快取 |
| `POST` | `/build-cache/save` | 儲存 build 快取條目 |
| `GET` | `/dialog/file` | 開啟系統檔案選擇對話框（回傳選取路徑） |
| `POST` | `/bridge/shutdown` | 關閉 bridge server 程序 |

### 三種模式 / Assistant

| 模式 | 使用的 Assistant | Toolkit | 用途 |
|------|------------------|---------|------|
| **Chat Mode**（預設） | `sighting_assistant` | sighting | HSD 分析、與程式碼變更關聯 |
| **Log Analysis Mode** | `displaydebugger` | displaydebugger | Log 分析、Display driver 除錯 |
| **GFX/GOP Regression** | `sherlog`（+ QuickBuild） | sherlog | GPU driver / GOP firmware regression 分析 |

---

## 開發人員：建置安裝檔

> 一般使用者不需要此步驟，僅在需要修改程式碼後重新打包時使用。

### 額外需求

| 項目 | 說明 |
|------|------|
| **Python 3.10+** | 需加入 PATH（不可用 Windows Store 版）|
| **pywinpty** | `pip install pywinpty` |
| **PyInstaller** | `pip install pyinstaller`（首次執行 build.ps1 會自動安裝）|
| **Inno Setup 6** | 下載：https://jrsoftware.org/isinfo.php |

### 建置步驟

從專案根目錄執行：

```powershell
.\installer\build.ps1
```

build.ps1 會自動：
1. 確認 Python、pywinpty、PyInstaller 已安裝
2. 以 PyInstaller 將 `bridge_server.py`、`native_host.py`、`check_env.py` 打包成獨立 `.exe`
3. 呼叫 Inno Setup 產生安裝檔（內含 `bridge_server.exe`、`native_host.exe`、`extension/`）
4. 將 `check_env.exe` 複製到輸出目錄（獨立工具，不打包進安裝檔）

輸出：
- `C:\Intel\Chat_Mode_Assistant_Setup.exe` — 主安裝檔
- `C:\Intel\check_env.exe` — 獨立環境檢查工具

### Extension ID（setup.iss）

Extension 的 `manifest.json` 內含固定 `key`，因此 Extension ID 恆為：

```ini
#define ExtensionId "pmbnnkfhdkommfpphknjpppmlmbihomi"
```

此 ID 已寫死在 `setup.iss`，安裝時用於產生 `nm_manifest.json` 的 `allowed_origins`。除非更換 `manifest.json` 的 `key`，否則不需修改。

---

## 手動啟動 Bridge（開發 / 除錯用）

不想使用 Native Messaging 自動啟動時，可直接執行：

```powershell
# 從 source 執行（需要 Python）
cd bridge
python bridge_server.py

# 或執行打包好的 exe（不需要 Python）
%LOCALAPPDATA%\ChatModeAssistant\bridge_server.exe
```

Bridge 啟動後，Chrome Extension 會自動偵測並連線（Port 8776）。

---

## 已知問題與解決方法

| 問題 | 原因 | 解決方法 |
|------|------|----------|
| 狀態顯示 "No Bridge" | Native Messaging 未正確設定 | 重新執行安裝檔，確認 Registry 中已註冊 `com.chat_mode_assistant.bridge` |
| Extension 無法連線 | Extension 未正確載入，或 ID 不符 | 確認 Extension ID 為 `pmbnnkfhdkommfpppmlmbihomi`（由 manifest `key` 固定） |
| Session 無法建立 | `dt` 不在 PATH 或未完成 `dt gnai` 設置 | 先執行 `check_env.exe` 檢查，確認 `dt gnai` 可正常執行 |
| Toolkit 未安裝 | 尚未 register 對應 toolkit | 執行 `check_env.exe` 並點選 **▶ Install**，或手動執行 `dt gnai toolkits register ...` |
| 安裝時提示需要管理員 | 系統策略限制 | 選擇「Install for current user only」繞過 |

---

## Check GFX/GOP Driver Regression 功能說明 

GFX/GOP Driver Regression 目前還在測試中，可能無法正常使用

### 使用方式

在 Side Panel 內，對話視窗頂端有兩個按鈕：

| 按鈕 | 功能 |
|------|------|
| **GFX** | 對當前 HSD 進行 GFX Driver Regression 分析 |
| **GOP** | 對當前 HSD 進行 GOP Firmware Regression 分析 |
| **H** | 開啟 History 面板（顯示過去的查詢結果）|
| **C** | 清除當前 HSD 的所有 Regression 記錄 |

### 查詢流程

```
點擊 GFX / GOP
  ↓
① 同一 HSD History lookup（by hsd_id）       ← 最快，直接顯示
  ↓（沒找到）
② Cross-HSD History lookup（by fail/pass 版本）← 其他 HSD 有相同版本直接借用
  ↓（沒找到）
③ Per-version Build Cache（組裝個別 build）  ← 跳過 QuickBuild 登入
  ↓（沒找到）
④ QuickBuild 登入後重新查詢
```

### 第一次使用前提

- 需要有 QuickBuild 帳號（Intel 內部網路）
- 第一次查詢每個版本組合時，會彈出 QB 登入視窗
- 查詢結果會自動存入本機 History Cache，後續相同版本不需重新登入

### History Cache 存放位置

安裝後 cache 存放於安裝目錄：

```
%LOCALAPPDATA%\ChatModeAssistant\
├── driver_history_gfx.json   ← GFX 查詢紀錄
├── driver_history_gop.json   ← GOP 查詢紀錄
├── build_cache_gfx.json      ← GFX per-version build 快取
└── build_cache_gop.json      ← GOP per-version build 快取
```

---

## 環境變數（可選）

| 變數 | 預設值 | 說明 |
|------|--------|------|
| `BRIDGE_PORT` | 8776 | Bridge Server 監聽 Port |
| `BRIDGE_HOST` | 127.0.0.1 | Bridge Server 監聽 IP |
| `BRIDGE_ASSISTANT` | sighting_assistant | 預設 GNAI Assistant 名稱 |
| `BRIDGE_DT_PATH` | （空，自動搜尋） | 指定 dt CLI 路徑 |
| `BRIDGE_DEBUG` | 1 | 啟用 debug log |

---

## 安裝目錄結構（安裝後）

```
%LOCALAPPDATA%\ChatModeAssistant\
├── bridge_server.exe          ← Bridge Server（獨立執行，不需 Python）
├── native_host.exe            ← Native Messaging Host（Chrome 自動呼叫）
├── nm_manifest.json           ← Native Messaging 設定（安裝時自動生成）
├── extension\                 ← Chrome Extension（Load unpacked 此資料夾）
│   ├── manifest.json
│   ├── background.js
│   ├── sidepanel.html
│   ├── sidepanel.js
│   ├── regression_ui.js
│   └── icons\
│
│  ── 以下為執行期自動產生，安裝後不存在 ──
│
├── bridge.port                ← Bridge 實際監聽埠號（bridge_server 啟動時寫入）
├── bridge_debug.log           ← Bridge debug log（BRIDGE_DEBUG=1 時產生）
├── bridge.pid                 ← Bridge 程序 PID（解除安裝時自動清除）
├── driver_history_gfx.json    ← GFX regression 查詢紀錄（首次查詢後產生）
├── driver_history_gop.json    ← GOP regression 查詢紀錄（首次查詢後產生）
├── build_cache_gfx.json       ← GFX per-version build 快取（首次查詢後產生）
└── build_cache_gop.json       ← GOP per-version build 快取（首次查詢後產生）
```

> `check_env.exe` 不隨安裝檔安裝，為獨立工具，請在安裝前單獨執行。

## 原始碼結構（開發人員）

```
Chat_Mode_Assistant_tool/
├── extension/                  ← Chrome Extension 原始碼
│   ├── manifest.json
│   ├── background.js           ← Service Worker（NM 啟動、HTTP/SSE 連線、keep-alive）
│   ├── sidepanel.html
│   ├── sidepanel.js            ← Side Panel UI 主邏輯
│   ├── regression_ui.js        ← GFX/GOP Regression 面板 UI
│   └── icons/
├── bridge/                     ← Bridge Server 原始碼
│   ├── bridge_server.py        ← HTTP/SSE 伺服器，管理 dt gnai chat 程序
│   ├── native_host.py          ← Chrome Native Messaging Host（啟動 bridge）
│   ├── requirements.txt
│   ├── run_bridge.ps1          ← 手動啟動 bridge（開發 / 除錯用）
│   ├── run_bridge.cmd
│   ├── install_native_host.ps1 ← 手動安裝 Native Messaging（不使用安裝檔時）
│   └── com.chat_mode_assistant.bridge.json.example  ← NM manifest 範例
├── installer/                  ← 安裝檔建置工具
│   ├── build.ps1               ← 一鍵建置（產生 Setup.exe + check_env.exe）
│   ├── setup.iss               ← Inno Setup 設定
│   ├── bridge_server.spec      ← PyInstaller spec（bridge_server.exe）
│   ├── native_host.spec        ← PyInstaller spec（native_host.exe）
│   ├── check_env.spec          ← PyInstaller spec（check_env.exe）
│   ├── check_env.py            ← 環境檢查工具原始碼
│   ├── configure.py            ← Extension ID 設定工具（目前未隨安裝檔散佈）
│   └── dist/                   ← PyInstaller 輸出（build.ps1 產生）
├── external/                   ← 外部模組（git submodule）
│   ├── Check-gfx-driver-regression/  ← GFX/GOP Regression 核心邏輯
│   │   ├── regression_bridge.py      ← Bridge 端點整合層
│   │   ├── regression_checker.py     ← QuickBuild 查詢與比對邏輯
│   │   ├── regression_cache.py       ← History / build cache 管理
│   │   └── regression_ui.js          ← （已移至 extension/，此為原始版本）
│   ├── SightingAssistantTool/        ← sighting toolkit 原始碼（參考用）
│   ├── GNAI_GCD_AssistChatter/
│   └── Steve_Chatter/
├── design/                     ← 設計文件與架構說明
├── gen_regression_ppt.py       ← Regression 結果轉 PPT 工具
└── version.txt
```

